Библиотека глубокого обучения на CuPy с GPU-ускорением. Включает полносвязные, сверточные, рекуррентные (RNN, LSTM, GRU) слои, функции активации (ReLU, Sigmoid, Softmax), оптимизаторы (Adam, RMSProp), BPE-токенизатор, модули для обучения и сохранения моделей в формате HDF5.
